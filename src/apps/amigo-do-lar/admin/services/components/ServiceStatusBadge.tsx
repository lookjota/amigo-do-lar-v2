export function ServiceStatusBadge({ isActive }: { isActive: boolean }) { return <span className={`amigo-admin-status amigo-admin-status-${isActive ? 'completed' : 'cancelled'}`}>{isActive ? 'Ativo' : 'Inativo'}</span> }
